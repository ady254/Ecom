{
  "name": "minara",
  "version": "1.0.0",
  "private": true,
  "description": "MINARA Luxury Gifting E-Commerce Platform",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "rimraf": "^6.0.1",
    "prettier": "^3.4.2",
    "@types/node": "^22.10.5"
  },
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
